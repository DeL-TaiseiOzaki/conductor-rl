# Copy to .env and fill in. .env is gitignored.

# OpenRouter — the worker pool (required)
OPENROUTER_API_KEY=
# HuggingFace — for scale-up dataset curation (optional)
HF_TOKEN=

# Judge model for HLE / CharXiv-style open-ended grading (later phases, optional)
JUDGE_API_KEY=
